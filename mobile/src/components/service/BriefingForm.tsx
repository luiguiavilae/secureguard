// TODO: Formulario de briefing del servicio — descripción, requerimientos especiales, instrucciones
import React from 'react';
import { View } from 'react-native';

interface BriefingFormProps {
  value: string;
  onChange: (text: string) => void;
}

export function BriefingForm({ value, onChange }: BriefingFormProps) {
  // TODO: Implementar formulario de briefing
  return <View />;
}
