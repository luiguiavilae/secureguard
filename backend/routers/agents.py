# TODO: Implementar endpoints de gestión de agentes
# Endpoints: GET /nearby, GET /{agent_id}, PATCH /profile, PATCH /availability,
#            POST /documents, GET /reviews, GET /earnings
from fastapi import APIRouter

router = APIRouter()
