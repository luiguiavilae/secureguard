# TODO: Implementar endpoints de mensajería in-app
# Endpoints: GET /{service_id}/messages, POST /{service_id}/messages
# Realtime vía Supabase channels (no polling)
from fastapi import APIRouter

router = APIRouter()
