# TODO: Implementar endpoints de reseñas y calificaciones
# Endpoints: POST / (crear review), GET /agent/{agent_id}, GET /client/{client_id}
# Solo se puede crear review una vez finalizado el servicio
from fastapi import APIRouter

router = APIRouter()
