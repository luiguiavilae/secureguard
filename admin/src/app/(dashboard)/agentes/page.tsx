// TODO: Página lista de agentes verificados — búsqueda, filtros por estado/distrito, acciones
import React from 'react';

export const metadata = { title: 'Agentes — SecureGuard Admin' };

export default function AgentesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Agentes</h1>
      {/* TODO: Implementar tabla de agentes */}
    </div>
  );
}
